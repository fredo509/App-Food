<!DOCTYPE html>
<html>
  <head>
    <title>Trading Forum</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= turbo_include_tags %>
  </head>
  <body>
    <div class ="layout-errors">
      <% if alert.present? || notice.present? %>
        <div class="alerts">
          <%= alert %>
          <%= notice %>
        </div>
      <% end %>
    </div>
    <% if user_signed_in? %>
      <% puts '@@@@@@@@@@@@@@@@@@@@'  %>
      <% puts current_user.name %>
      <% puts '@@@@@@@@@@@@@@@@@@@@'  %>
    <% else  %>
      <% puts '@@@@@@@@@@@@@@@@@@@@'  %>
      <% puts 'No user signed in' %>
      <% puts '@@@@@@@@@@@@@@@@@@@@'  %>
    <% end %>
    <div class="layouts-application-navbar">
      <% if user_signed_in? %>
        <%= button_to "Sign out", destroy_user_session_path, :method => :get, class: "btn" %>
        <%= link_to "Config", edit_user_path(current_user.id) %>
        <h2> <%= current_user.name %> </h2>
      <% else  %>
        <%= link_to "Sign in", new_user_session_path, class: "btn" %>
        <%= link_to "Sign up", new_user_registration_path, class: "btn" %>
      <% end %>
    </div>
    <%= yield %>
  </body>
</html>
